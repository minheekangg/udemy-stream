import React from 'react';
import { BrowserRouter, Route } from 'react-router-dom';

const App =  () => {
    return (
        <BrowserRouter>
            <div>
                <Route path="/" exact component={} />
            </div>
        </BrowserRouter>
    )
}

export default App;